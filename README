To generate graphs from NellBalloons data:

find data -name NellBalloons.csv | xargs src/scatter.js -G -g -y all
for f in *-all.gnuplot ; do gnuplot $f ; done
psjoin *-all.ps | psnup -l -n4 > output.ps
